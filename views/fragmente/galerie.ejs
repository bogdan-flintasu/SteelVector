<section class="galerie-wrapper">
  <% imagini.forEach((img) => {
    const caleRel = img.cale_relativa || img.fisier;
    if (!caleRel) return;
    const extIndex = caleRel.lastIndexOf('.');
    const numeBase = extIndex >= 0 ? caleRel.slice(0, extIndex) : caleRel;
    const extStr = extIndex >= 0 ? caleRel.slice(extIndex) : "";
    const altText = img.alt || img.nume || img.titlu || "";
  %>
  <figure class="galerie-item">
    <picture>
      <source media="(max-width: 767px)" srcset="<%= caleGalerie %>/<%= numeBase %>_sm<%= extStr %>">
      <source media="(max-width: 1099px)" srcset="<%= caleGalerie %>/<%= numeBase %>_md<%= extStr %>">
      <img src="<%= caleGalerie %>/<%= caleRel %>" alt="<%= altText %>" title="<%= img.descriere || img.nume || img.titlu || "" %>" loading="lazy">
    </picture>
    <figcaption>
      <span class="index-imagine"></span><%= img.descriere || img.nume || img.titlu || "" %>
      <% if (img.atribuire) { %>
        <small class="atribuire">Foto: <a href="<%= img.atribuire.sursa %>" target="_blank"><%= img.atribuire.autor %></a> (<%= img.atribuire.licenta %>)</small>
      <% } %>
    </figcaption>
  </figure>
  <% }) %>
</section>
